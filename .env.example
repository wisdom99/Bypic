# Required for the AI mood-board fabric matcher.
# Without this, /api/match falls back to a deterministic heuristic so the demo never breaks.
# Get a key from https://aistudio.google.com/apikey
GEMINI_API_KEY=
